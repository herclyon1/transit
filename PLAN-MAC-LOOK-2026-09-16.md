# 新版 vs 苹果地图截图：比对结论与改法方案（2026-09-16 08:2x，待你过）

比对图：`compare-maps-vs-cost.jpg`（上 = 你 09-16 03:09 发的地图 App「Pinned」面板截图；下 = 新版薪资页 Mac 1280×744）。已推到企业微信。

## 一、比对结论

对上的（按像素量过你截图里的字高，换算到逻辑像素）：卡片标题 22 Bold ✓、卡片段头 15 Semibold ✓、侧栏行标题 13 / 副题 11 ✓、侧栏段头 11 Bold ✓、侧栏 200 通高无圆角 ✓、卡片 320 浮在地图上 ✓、右列 36 玻璃钮 @右 8 ✓、行高 32 ✓。

还不像的，按肉眼从大到小：

| # | 部位 | 苹果地图（你的截图） | 新版 | 差在哪 |
|---|---|---|---|---|
| 1 | 底图 | 地形阴影 + 海深渐变 + SF 字体 + 英文地名 | 平的矢量样式（dddevid 的 Apple 配色）+ Noto + 中文 | **最大的一眼差**：没有山体阴影和海深 |
| 2 | 侧栏行 | 32 高里两行（标题 13 + 灰副题 11）+ 22 圆图标；选中行常亮 | 昨晚是单行；今早已改成两行 + 选中常亮（未提交） | 图标是国旗 20，不是彩色圆 |
| 3 | 侧栏底 | 「Terms & Conditions ›」灰 11 | 「‹ 目录」蓝 13 | 颜色/字号 |
| 4 | 卡片标题 | 「Pinned」左对齐，和 × 同一行 | 居中在 × 下面（按「Marked Location」地点卡的 AX 实测） | Maps 两种都有；你截图是左对齐那种 |
| 5 | 卡片材质 | 更白（约 85%）、分组框是偏白的盒 + 发丝线 | 白 70% + 模糊、分组框黑 3% | 我们的更透、更灰 |
| 6 | 卡片底 | 36 胶囊工具条，28 圆钮 | 今早已加（换算成工时 / 来源 / 钉住），未提交 | — |
| 7 | 右列 | 6 颗分 4 组（账户 / 交通+定位 / ± / 指南针）+ 地图顶部比例尺 | 当地 + ± | 没有比例尺 |
| 8 | 窗口 | 标题栏 + 红绿灯 | 浏览器标签页 | 做不了（浏览器的） |

## 二、改法（和隔壁商定的顺序；每条你点头才做）

| 序 | 改什么 | 怎么改 | 估时 |
|---|---|---|---|
| 1 | **材质**（隔壁：这条不改，底图再像也不像） | 苹果的侧栏和卡片是半透明 vibrancy，地图的绿/蓝从底下透出来；我们侧栏 #fafafa 80% + 模糊 100（Kit 数字）几乎不透。改成白 72% + 模糊 40 + 饱和 1.4（照你截图的透色程度调，Kit 的 80/100 作废） | 0.5 h |
| 2 | 分组框 | 黑 3%（那是 iOS 的做法）→ 偏白盒 白 55% + 发丝线分隔（Apple 面板的做法） | 0.3 h |
| 3 | 侧栏行 | 两行 + 选中常亮（已做）；右侧数字「91 小时/月」改成灰色次级（Apple 侧栏 trailing 都是灰的，不然像表格）；底部「‹ 目录」改灰 11「目录 ›」 | 0.3 h |
| 4 | 卡片头 | 标题左对齐 + × 同一行（照你的 Pinned 截图；隔壁：有名字的地点卡 Apple 也是左上 Bold + × 右上，居中的「Marked Location」是无名掉针卡的特例）；材质白 85% | 0.5 h |
| 5 | 比例尺 | MapLibre ScaleControl 重样式成 Apple 那条 0 / 125 / 250 / 375 km 分段尺，放右上；「当地 ▾」套进同一 36 玻璃钮组；指南针不放（我们锁了旋转，HIG 不放没用的控件） | 0.5 h |
| 6 | 海深 | Natural Earth 10m bathymetry（公有领域）简化到 5% 后几 MB，分 3–4 档填色（近岸浅 → 远海深，取你截图的色），放 cost/data/，三张地图页共用 | 1 h |
| 7 | 山体阴影 | MapLibre raster-dem（encoding terrarium）+ hillshade 层，强度 0.25–0.35（国家尺度只是淡淡一层）。瓦片两种走法你选：**A** 直连 AWS 开放数据（无密钥、CORS 开，但多一个外部源，README 现在写「外部依赖只剩地理院」）；**B** 离线预烘 z0–6 进 tiles/ 一个 PMTiles（约 30–80 MB，和现有 pmtiles 同套路，不加外部依赖） | A 0.5 h / B 1.5 h |
| 8 | 底图标注小项 | 国名改灰色 + 字距加宽（Apple 的 NORTH KOREA 那种）；开 OpenFreeMap 的海名层（East China Sea 斜体灰） | 0.5 h |
| — | 不做 | 窗口红绿灯（浏览器的）、SF 字体做地图标注（许可；侧栏/卡片是 HTML，Mac 上 -apple-system 本来就是 SF）、3D/卫星、陆地植被色 | — |

做完两端跑 kit-audit + accept（Kit 数字表里侧栏材质那行按你截图改，注明依据换成实机截图），再发一张同样的并排图给你。

## 三、居住体验等级——划分标准草案（你说：排除便利；治安只有能反证「乱到该降级」才算）

按规则定级、不加权平均，每条都有公开数据源：

| 维度 | 怎么算 | 数据源 | 作用 |
|---|---|---|---|
| **主轴：用途地域** | 住居専用地域 → 基线 A；住居/準住居 → B；商業・近隣商業（歓楽街集中）→ C；工業・準工業 → C/D | 国土数値情報 A29 用途地域（国交省） | 「住在那儿每天什么感受」最客观的代理 |
| 环境 | 公園緑地面积率高 +1；幹線道路/高架/貨物线紧邻、伊丹机场航空機騒音等高线内 −1 | 国土数値情報 P27 公園、道路、国交省航空機騒音 | 安静、绿 |
| 灾害（只降） | 浸水 ≥3 m 占比大 −1；≥5 m 或土砂災害警戒区域 −2；液状化待解析 | A31a 浸水想定（河川単位 2025，已抓）、A33 土砂、大阪府液状化 PDF | 不淹 |
| 治安（只降、双证据） | 府警犯罪率府内前 5% **且** 有官方认定或多家报道（如 あいりん = 大阪市自己的施策区域）才降 1–2 级；否则不参考 | 大阪府警 令和 7 年 市区町村別認知件数（已抓）+ 官方文件 | 照你说的：能反证才算 |
| 住宅质量（可选） | SUUMO 在挂房源築年中位 ≥40 年 −1 | 按站房租脚本顺手取 | 老旧 |
| 不进等级 | 通勤可达（已独立成图）、超市密度（你说便利已独立）、口碑/文艺/体面 | — | — |

例：福島・野田 = 商業/近隣商業为主 → C，公園少不加，浸水中等 → C；帝塚山 = 第一種低層住居専用 → A，公園有 → S，无灾害扣分 → S；あいりん = 商業 → C，治安双证据 −2 → D（全府唯一）。
产出：`tiers_score.py` 改成这套规则，每块显示「基线 + 每项加减 + 来源」，页面上「作者标注」改回「居住等级」并可点开看依据。估 3 h（A29/P27/A33 下载解析 + 规则 + 页面依据行）。
