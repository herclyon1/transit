# transit 仓库规矩（每个会话每轮都读；细则在 WORKFLOW.md）

- 三个会话：验收（main 树）、界面（transit-ui / ui）、数据（transit-data / data）。干活会话只推自己的分支，main 只由验收会话合并。
- **干活会话不写中间回复**：工具调用之间不输出任何给人看的句子（中文英文都不要，「Now …」「接下来…」一律禁止）；功能单元做完前不结束回合、不问用户；结果发消息给验收会话（id `local_ffb898d8-d748-430a-81a4-6a04c98d4d32`），对话里只留一行「已发验收」。用户只看验收会话。
- 卡住发消息给验收会话，由它问用户。
- 给用户看的文字一律中文。
- 尺寸颜色必须有出处（Kit 画板、实机测量、苹果渲染器采样、样式表解出），说不出来源的不写。
- **派 agent（Agent 工具 / Workflow / 任何子代理）必须先经用户同意**，验收会话也不能代他同意；要派先发消息给验收会话，由它问用户。（用户死命令 2026-09-16）
- **仿苹果任何视觉效果，只用解出的原值**：来源限于样式表 .styl、着色库 default.metallib 的公式、进程内抓到的 uniform/纹理、CoreMaterial 材质配方、VectorKit 资源/常量。交付里每个数字必须写「来自哪个文件的哪一项」；写不出来源的数字**不许出现**，先发验收会话由数据会话去解；确实解不到才允许采样兜底，并在文档里标红「采样替代」。采样/拟合只用来验证。每个可见元素的来源与复刻方式记在 RENDER-PIPELINE.md。（用户 2026-09-16 晚两次强调）
- **复制的是逻辑，不是瓦片内容**（用户死命令 2026-09-17）：从苹果文件里解的是样式表、着色公式、材质色表、气候格、标注排布规则这类**规则**；苹果瓦片里的内容（地类栅格、气候码、高程、要素清单、每要素 min-zoom、名单）是**数据**，地图无限大抄不完，一律不进网站、不进 map/data。网站上的数据只来自第三方（OSM/OpenFreeMap、Natural Earth、MODIS、Köppen、terrarium、N02…），用解出的规则映射成苹果的类和级别；苹果瓦片内容只允许放仓库外做校准（映射命中率写 README）。已进仓库的苹果瓦片派生文件要从 map/data、ui/basemap 移除。

