# transit 仓库规矩（每个会话每轮都读；细则在 WORKFLOW.md）

- 三个会话：验收（main 树）、界面（transit-ui / ui）、数据（transit-data / data）。干活会话只推自己的分支，main 只由验收会话合并。
- **干活会话不写中间回复**：工具调用之间不输出任何给人看的句子（中文英文都不要，「Now …」「接下来…」一律禁止）；功能单元做完前不结束回合、不问用户；结果发消息给验收会话（id `local_ffb898d8-d748-430a-81a4-6a04c98d4d32`），对话里只留一行「已发验收」。用户只看验收会话。
- 卡住发消息给验收会话，由它问用户。
- 给用户看的文字一律中文。
- 尺寸颜色必须有出处（Kit 画板、实机测量、苹果渲染器采样、样式表解出），说不出来源的不写。
- **派 agent（Agent 工具 / Workflow / 任何子代理）必须先经用户同意**，验收会话也不能代他同意；要派先发消息给验收会话，由它问用户。（用户死命令 2026-09-16）
- **仿苹果任何视觉效果，只用解出的原值**：来源限于样式表 .styl、着色库 default.metallib 的公式、进程内抓到的 uniform/纹理、CoreMaterial 材质配方、VectorKit 资源/常量。交付里每个数字必须写「来自哪个文件的哪一项」；写不出来源的数字**不许出现**，先发验收会话由数据会话去解；确实解不到才允许采样兜底，并在文档里标红「采样替代」。采样/拟合只用来验证。每个可见元素的来源与复刻方式记在 RENDER-PIPELINE.md。（用户 2026-09-16 晚两次强调）
- **复制的是逻辑，不是瓦片内容**（用户死命令 2026-09-16）：从苹果文件里解的是样式表、着色公式、材质色表、气候格、标注排布规则这类**规则**；苹果瓦片里的内容（地类栅格、气候码、高程、要素清单、每要素 min-zoom、名单）是**数据**，地图无限大抄不完，一律不进网站、不进 map/data。网站上的数据只来自第三方（OSM/OpenFreeMap、Natural Earth、MODIS、Köppen、terrarium、N02…），用解出的规则映射成苹果的类和级别；苹果瓦片内容只允许放仓库外做校准（映射命中率写 README）。已进仓库的苹果瓦片派生文件要从 map/data、ui/basemap 移除。
- **拟合视野与验收视野分开**：按某视野校准的规则标「拟合替代」，必须在至少三个未参与拟合的视野上验命中率；派单只给「找规则、写出处」，不给截图名单当目标；数据类验收先问规则和命中率，最后看图（WORKFLOW §八）。

- **手机遥控页一律按「添加到主屏幕」的独立窗口（standalone，无地址栏）设计和验收**（用户死命令，早已如此，2026-09-18 补写）：index.html 保持 apple-mobile-web-app-capable / status-bar-style / viewport-fit=cover 与 manifest display=standalone；并排图、像素比对、模拟器验收一律在主屏独立窗口形态下截，带地址栏的浏览器截图不算验收图；安全区用 env(safe-area-inset-*)。
- **遥控页是用户一人私用、有密码门、不分发**（2026-09-18）：SF 字体自托管给 Android 照用，不再以许可/分发/风险为由拦任何资源；验收标准是 iOS 上与原生逐像素一致，Android 不单独验收。
- **死命令（2026-09-19）**：网页在 Mac / iOS / 安卓上是同一个效果；用户在任何设备上发现的问题一律是我们没做好，禁止以「安卓 / Chrome / 手机环境」为由解释或推脱。

- **沟通只留监督局（用户 2026-09-19 03:5x 定）**：用户只和「transit 监督局」会话（`local_54bbaa25-40cf-4447-8fc3-637c0c62ffee`）沟通；验收会话不再写给用户看的话，结果一律发监督局，对话里只留「已发监督局」；验收做任何事、问用户任何事先经监督局点头，监督局可替用户决定。

- **红线（用户 2026-09-19 04:2x）**：不要任何拟合值、不自己猜、不自己推导。苹果实现的每一个效果，先反编译把实现原理搞清楚，再琢磨怎么搬到网页。交付里的数只许是反编译原值、探针读到的原值、渲染器输出采样（只作校验/标红兜底）；「拟合」「推导」「假设」不许作为产物出现。

- **工具与 agent 由监督局判断（用户 2026-09-19 04:3x 全权委托）**：需要就建，不浪费时间即可，怎么判断全看监督局；干活会话建脚本不必先报，交付时写一句用途；派 agent 先报监督局。
