# 原始取数记录

生活类数据的原始来源。城市 JSON 里每个 `listing` 数字的 note 都能对回这里的某一行。

```
cost/data/raw/<city>/<yyyymmdd>/<platform>.jsonl      # 手机 App 无障碍树一键取数（pipeline/cost/phone_grab.py）
cost/data/raw/<city>/<yyyymmdd>/anjuke_web_<站>.json  # 网页版列表（浏览器里 JS 抽取）
```

## 怎么取（乌鲁木齐 2026-09-14/15 实测定型）

手机 USB 连 Mac，App 自己登录好。用户在手机上搜好词、停在列表页第一屏，Mac 上跑一条：

```
python3 pipeline/cost/phone_grab.py               # 自动认前台 App，取前 10，追加到当天的 <app>.jsonl
python3 pipeline/cost/phone_grab.py --n 30 --scroll 5   # 原生列表（贝壳）屏幕外条目不在树里，自动下滑 5 屏合并
```

| 项目 | App / 页面 | 操作 | 备注 |
|---|---|---|---|
| 鸡蛋、牛奶、大米、面包、可乐、啤酒 | 多多买菜（拼多多 App 内） | 搜词，综合排序，第一屏 | 结果页是自研内核，**必须开读屏（TalkBack）**才读得到 |
| 面粉、食用油、土豆、西红柿、白菜、鸡腿、羊肉、苹果、馕 | 同上 | 同上 | 还没取，篮子九项空着 |
| 本地一顿饭（拌面） | 美团 → 特价团（到店团购） | 定位 南门(地铁站)，搜"拌面"，默认排序 | 原生页面，不用读屏；用户说本地吃饭用特价团比外卖便宜 |
| 巨无霸套餐 | 麦当劳 App → 到店取餐 → 选城市/门店 | 菜单 → 巨无霸/牛鱼肉堡 → 套餐 | 我用 adb 自己点的 |
| 拿铁 | 瑞幸 App → 自提 → 选城市/门店 → 菜单 → 大师咖啡 | 取菜单标价，券后价写 note | 打烊的店选不进菜单 |
| 大阪 面粉/油/土豆/西红柿/白菜/鸡腿/猪肉/苹果 | イオンネットスーパー 野田阪神店（网页，不登录） | `python3 pipeline/cost/aeon_grab.py` 抓 おすすめ順第一屏 → `basket_from_aeon.py` 入篮 | 蔬果按个/切块卖不标重量，折 kg 用假定重量（脚本 WEIGHT，how 里写明）；总务省 2026-02 大阪市每 kg 统计价做对照；羊肉用豚小間切れ顶 |
| 四档房租 | 安居客 App → 租房 → 合租/整租 → 地铁站 | 没有价格排序：默认排序全部翻完（连续两屏无新条目为止），每站一跑；郊区站三站合选 | 贝壳 09-16 起弃用（列表混商用楼太多）；整租暂用 09-15 贝壳那份，how 里注明，安居客整租翻完后换。安居客网页 09-15 起弹 58 反爬页，不绕 |

## 篮子怎么算（`pipeline/cost/basket_from_grab.py`）

综合排序前 10 条 → **先剔掉不是同一种东西的**（脚本里每键一条 `EXCL` 正则：鸡蛋剔乌鸡蛋/卤蛋/鹌鹑蛋/沥水篮，牛奶剔酸奶/奶粉/整件装，面包剔蛋糕/饼干，可乐剔啤酒/百事整件，啤酒剔精酿/12 罐箱……）→ 按商品名里的规格折到篮子单位 → 去掉离中位 2 倍以外的 → 取中位。写进城市 JSON 的 `how` 会列出剔了哪几条和 n。不剔的话「鸡蛋」中位会被乌鸡蛋和卤蛋抬到 10 元（2026-09-14 乌鲁木齐实测 8.33）。房租不走这条（`pipeline/cost/rent_from_beike.py`）：安居客默认排序全量 → 去重、去商用楼/写字间（只看小区名段）、整租再去面积 <15/>80 ㎡ 和 ≥6000 → 取便宜的四分之一处；同样本中位写 compare。

个人信息（姓名、电话、地址）不会进树，jsonl 里只有商品/房源字段。
