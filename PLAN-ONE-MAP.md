# 唯一地图：功能清单（草稿，等用户过）

> 2026-09-16 用户拍板：transit 合成**唯一一张地图**。不是把四个页面搬进一张图，是把四页的**功能**在唯一地图上重新做出来；架构也不沿用（shell.js / hig.css / 四个 index.html / 按页探针都不是起点）。
> 沿用的只有事实：原始数据与管线产出（reach、tiers、cities/*.json、pmtiles）、Kit 数字表、Maps 实测组合尺寸。数据的形状也按新功能重审。
> 用户的话：「功能实现方式是廉价的，本身的功能才值钱。」所以这张表按 **目的 → 在唯一地图上做什么动作 → 看到什么** 写，不写旧页的菜单项。

## 0. 这张地图是给谁、干什么

一个人（用户自己）在日本/东亚生活：要**选城市**（哪里挣得多花得少）、**选住处**（在大阪住哪通勤能接受、房租多少）、**认地方**（把行政区划背下来、看清层级）。三件事发生在同一张地图的不同缩放上：世界 → 城市 → 街区。

## 1. 目的 A：比较城市——外国人真实能挣多少、要花多少

| 目的 | 在地图上做什么 | 看到什么 | 事实数据 |
|---|---|---|---|
| 一眼看哪座城市划算 | 缩到东亚/世界 | 有数据的城市是实心点，点旁写「N 小时/月」（房租+通勤+饮食 ÷ 时薪）；待调查是空心点 | cities.json、各城市 JSON |
| 看一座城市的账 | 点城市点 / 侧栏选城市 | 地点卡：工时换算（月开销 ÷ 时薪）→ 时薪（真实招聘帖中位）→ 房租档 → 通勤月票 → 饮食费（在岗 / 过渡期）→ 能干什么活（签证规则）→ 岗位帖原话 → 来源 | 同上 |
| 换成自己的货币比 | 卡上切币种 | 所有数字换币，帖子原话附换算 | rates.json |
| 两城并排比 | 钉住一座再点另一座 | 两张卡并排，同一行对同一行 | — |
| 知道数字怎么来的 | 点任一数字 | 来源·平台·日期·样本数·「怎么取的」一句，链接原页 | 每条 source/how/items |

**形态（界面会话补）**——一律用 Maps 已有的形，不发明：
- 缩到东亚/世界：Maps 的 z≤4 那种视图，底图带海深/山影（数据会话在做）；城市点 = Maps 的城市标注点形（黑点白描边）+ 名字，「N 小时/月」作第二行小字（Maps 的 POI 标注可以两行：名 + 类别），待用户定（第 6 节 1）。
- 城市卡 = Maps **有名地点卡**（实测 09-16：标题 22 Bold 居中距顶 48、28 圆钮 @12、主钮 288×45、段头 15 Semibold、键值行无框发丝线 48、次钮 288×36、卡底 36 工具条 3×28）：主钮 = 「在这里找住处」（缩进大阪、切找住处模式，只有大阪有）；段 = 换算成工时 / 能干什么活 / 岗位 / 来源；卡底工具条 = 钉住（★）、换币（¥ 弹 Kit 菜单）、分享链接。
- 侧栏（Mac）= Maps 侧栏 200：段头「城市」+ Medium 32 两行行（名 + 副题「留学签 · 周 28h」）+ 右灰数字「91 小时/月」，选中常亮；「待调查」可折叠段；iPhone = Sheet 中档列表（Kit Rows 68）。
- 换币 = 卡底工具条 ¥ 钮弹 Kit Menu（Mac）/ iOS UIMenu；不再放右列。
- 两城并排（Mac）= 第二张卡贴在第一张右边 8（Maps 没有这种，取 Kit Utility Panel 并排的形，写明是自定）；iPhone = 叠放第二张 Sheet。
- 点数字看来源 = 行右端「↗」（Kit Rows › Link 配件）→ 展开一行 Footnote：平台 · 日期 · 样本 · 怎么取的 + 链接。

## 2. 目的 B：在大阪选住处——住哪通勤能接受、房租多少

| 目的 | 在地图上做什么 | 看到什么 | 事实数据 |
|---|---|---|---|
| 定我要去的地方和能忍的时间 | 缩进大阪，选目的地（梅田/難波/天王寺/京橋，或四个都看），拖时间 | 只画满足条件的车站；点色 = 到目的地的时间档（≤15/≤25/≤40） | reach.json |
| 看某站住起来怎么样 | 点车站 | 车站卡：到各枢纽分钟（带出处等级、低频线/特急券标记）、**这一站**的单间月租（SUUMO 下四分位）、步行 5/10/15/20 分圈 | reach、station_rent、walk_polys |
| 不在车站上也能查 | 长按/右键任意地点 | 该点「步行到最近站 + 轨道」到四枢纽的时间 | reach + 步行路网 |
| 看一片区域的通勤面 | 打开面覆盖 / 势力范围 | 全府按最快枢纽上色（等时圈插值）；交界线 | reach + walk_polys |
| 知道这片住起来的体验 | 打开「作者标注」层 | 104 块的作者印象等级（数据分等拍板） | tiers.geojson、tiers_scored |
| 通学的钱 | （待做）定目的地后看通学定期 | 各社 1 か月通学定期 | pass_student、km_pairs |

**形态（界面会话补）**：
- 进大阪 = 缩到 z≥9 自动进「找住处」模式（Map Modes 里也能手选）；Maps 交通模式配色（灰路、彩色轨道、小站点，数据会话在做）。
- 目的地 + 时间 = 侧栏顶一张 Kit Forms 卡：「目的地」Pop-up（四枢纽都看 / 梅田…）+「到枢纽的时间」行 + 滑块；点色 = 时间档（≤15 绿 / ≤25 黄 / ≤40 橙 = iOS 系统色）；车站点 = Maps 交通模式的站点大小（2–4 px 线色小点，可点区域 ≥ 44/24）。
- 车站卡 = 同一张地点卡形：标题站名 + 副题「运营商 · 最短 N 分」+ 主钮「到目的地 N 分」（Maps「43 min」那颗，图标在上）；段「到各枢纽」四行（出处等级作副题）、「房租」一行（这一站 SUUMO 下四分位 + 样本数，↗ 到来源）、「步行圈」开关行（Kit 行 + 复选框/开关）；卡底工具条 = 钉子（在此落针）、比较、分享。
- 长按/右键 = Maps 的「Marked Location」卡（标题居中「已标记地点」，主钮「到目的地 N 分」，段「到各枢纽」）；钉子 = Maps 红针。
- 面覆盖 / 势力范围 = Map Modes 弹窗的三个 tile（车站 / 面覆盖 / 势力范围，已按实机做过一版可照搬形）；「作者标注」「低频线」「枢纽标记」= 弹窗白盒里的复选框（Mac）/ 开关（iPhone，实机 09-16 09:07 卫星下就是开关）。
- 通学定期（待做）= 车站卡里「通学定期」一行，按目的地和各社规则算 1 か月，↗ 到 pass_student 的来源。

## 3. 目的 C：把日本和东亚的行政区划学会

| 目的 | 在地图上做什么 | 看到什么 | 事实数据 |
|---|---|---|---|
| 看清从国到町丁目的层级 | 缩放：全国 → 県 → 市町村 → 政令市の区 → 町丁 | 边界随缩放换层级，点任一块看它属于谁、多少人口世帯 | japan pmtiles、国勢調査 |
| 认东亚 27 国的一级区划 | 平移到邻国 | 各国代表色、一级区划边界、首府名标在首府上；争议区按实际管理填色、官方划法虚线 | 東亜 pmtiles |
| 看交通骨架 | 打开交通层 | 铁路（高铁/普铁/地铁）、空港、港湾、口岸、航路 | N02/C28/N09/S12、OSM |
| **把県背下来** | 对当前视野发起「考我」 | 视野里的标签藏起来，逐个问名字/県厅（或反过来指位置）；答错的记下来 | 県界、県庁 |
| 按地方一关关过 | 选一个地方（近畿…）→ 考 | 全过后提示第 3 天、第 7 天再来（间隔重复） | 同上 |
| 看 1942 年的版图 / 海域 | 切时代图层 | 大日本一级区划、EEZ 近似、远岛 | （旧クイズ有一版，缺正经历史 GIS） |

**形态（界面会话补）**：
- 层级随缩放 = 底图本身（pmtiles 边界层按 z 换级），不是开关；点一块 = 地点卡（标题：块名，副题：属于谁；段：人口·世帯·面积；「上一级 / 下一级」用卡里的列表行（Maps「Connections」那种）跳）。
- 东亚 27 国 = 同一底图的邻国层；「只看某国」= 卡上主钮「只看此国」/ 卡底工具条（已按实机做过一版）；争议区虚线在底图里。
- 交通层 = Map Modes 弹窗白盒复选框（铁路 / 空港 / 港湾 / 口岸 / 航路），和目的 B 的开关同一个盒子按模式换内容。
- 「考我」= Map Modes 的一个 tile「考我」：进入后视野内标签隐藏，侧栏变成题卡（Kit Sheet 中档 / Mac 侧栏）：「这是哪个県？」+ 四选一（Kit Buttons Medium 34 胶囊，Mac Regular 24）或点地图；答错的县在侧栏「错题」段累积（Kit 列表行）；进度按地方记在 localStorage，第 3/7 天提醒用侧栏顶一行 Footnote（不做推送）。形态二选一（第 6 节 2）：藏标签逐个问 vs 给名字点位置——我建议两种都做成题型切换（Pop-up），不是两个模式。
- 1942 / 海域 = Map Modes 白盒里的「时代」复选框组（互斥单选用 Kit 分段），待用户定是否保留。

## 4. 所有目的共用

| 目的 | 做什么 | 看到什么 |
|---|---|---|
| 分享/回到某个状态 | 复制链接 | URL 带 模式/缩放/中心/选中/图层开关，打开即还原 |
| 找地方 | 侧栏搜索 | 城市、车站、県/市町村统一搜 |
| 换底图 | Map Modes 弹窗 | 模式 tile 单选（找住处 / 购买力 / 学习）+ 白盒复选框叠加项 + 底图出处 |
| 手机和 Mac 各自像原生 | — | iOS Sheet 三档 / Mac 侧栏 + 浮动卡；尺寸只从 Kit 和实机取 |

**形态（界面会话补）**：
- 状态 = URL hash：`#m=<mode>&z=&c=<lng,lat>&sel=<id>&layers=a,b&dest=&t=`，进页即还原；验收探针按状态跑（同一 URL 两端各一份期望值）。
- 侧栏（Mac 200 通高，材质 白 78% 模糊 40 饱和 1.8）：顶 = 搜索框 Kit XL 36（Maps 位 (15,47)），下面按模式换段：城市 / 车站结果 / 行政区。iPhone = Kit Sheet 三档（小 96 / 中 44% / 大），头 = 搜索胶囊 44。
- 右列（Mac，36 玻璃钮 @右 8 顶 8 间 6）：模式钮（弹 Map Modes）、± 一组、定位（有的话）；比例尺在地图顶（数据会话做）。iPhone = 右上 44 圆钮组。
- 地点卡（Mac 320 浮动 @208/8/8，白 86% 模糊 40 饱和 1.8；iPhone = 叠放 Sheet）：一套组件，段落按模式装。
- Map Modes 弹窗（Mac 320 Popover / iPhone Sheet）：tile 单选 = 模式（购买力 / 找住处 / 学习 / 考我），白盒 = 当前模式的叠加项，底部出处。
- 数字来源统一形：行右端 ↗ + 展开 Footnote；「作者标注」类无出处的内容一律标灰字「无出处」。

## 5. 明确不做的（从旧页里砍掉）
- 旧页的「浏览 / 闯关 / 混考」按钮分区（并入 3 里的「考我」和「按地方过」）。
- 「可达枢纽数 ≥N」分段（单枢纽时无意义，四枢纽时用点大小表达）。
- 「仅低频线可达」独立图例（并进低频线开关说明）。
- 首页卡片目录（一张图没有目录，侧栏就是入口）。

## 6. 待用户定
1. 目的 A 的「N 小时/月」要不要直接写在点旁（Maps 不这么做；我们的点本来就是数据）。
2. 目的 C 的「考我」形态（藏标签逐个问 vs 给名字点位置）。
3. 时代图层（1942）要不要保留。
